#### Terminal
git add *
git commit -m "changes made"
git push -u origin master

*only currently selected/opened file will be committed to git.

*Pull files from github back to c9*
git pull origin master